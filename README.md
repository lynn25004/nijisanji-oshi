# data branch

此 branch 只放 `streams.json`，由 `.github/workflows/build_streams.yml` 每 15 分鐘自動更新。

前端從 `https://raw.githubusercontent.com/lynn25004/nijisanji-oshi/data/streams.json` 抓取。

不要直接 merge 到 main！
